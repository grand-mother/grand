branches:
  only:
    - master

install:
  - source setup.sh && ./.travis/install.sh

script:
  - source setup.sh && ./.travis/script.sh

after_success:
  - source setup.sh && ./.travis/after-success.sh
